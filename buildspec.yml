version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.11
  build:
    commands:
      - echo "Zipping lambda"
      - zip function.zip lambda_function.py appointments.json
artifacts:
  files:
    - function.zip
